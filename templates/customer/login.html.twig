<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Iniciar Sesión - SFStore</title>
    
    <!-- Favicon -->
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>🛒</text></svg>">
    
    <!-- Stylesheets -->
    {{ encore_entry_link_tags('app') }}
</head>
<body class="bg-light">
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand fw-bold" href="{{ path('home') }}">
                <i class="fas fa-store me-2"></i>SFStore
            </a>
            <div class="navbar-nav ms-auto">
                <a class="nav-link" href="{{ path('customer_register') }}">
                    <i class="fas fa-user-plus me-2"></i>Registrarse
                </a>
            </div>
        </div>
    </nav>

    <div class="container my-5">
        <div class="row justify-content-center">
            <!-- Login Form -->
            <div class="col-lg-5 col-md-7">
                <div class="card shadow-lg border-0">
                    <div class="card-header bg-primary text-white text-center">
                        <h2 class="h4 mb-0">
                            <i class="fas fa-sign-in-alt me-2"></i>Iniciar Sesión
                        </h2>
                    </div>
                    
                    <div class="card-body p-4">
                        {% if error %}
                            <div class="alert alert-danger" role="alert">
                                <i class="fas fa-exclamation-circle me-2"></i>
                                {{ error.messageKey|trans(error.messageData, 'security') }}
                            </div>
                        {% endif %}

                        {% if app.session.flashbag.peek('success')|length > 0 %}
                            {% for message in app.session.flashbag.get('success') %}
                                <div class="alert alert-success" role="alert">
                                    <i class="fas fa-check-circle me-2"></i>{{ message }}
                                </div>
                            {% endfor %}
                        {% endif %}
                        
                        <form action="{{ path('customer_login') }}" method="post">
                            <div class="mb-3">
                                <label for="username" class="form-label fw-semibold">
                                    <i class="fas fa-envelope me-2"></i>Correo Electrónico
                                </label>
                                <input 
                                    type="email" 
                                    id="username" 
                                    name="_username" 
                                    class="form-control form-control-lg" 
                                    value="{{ last_username }}" 
                                    required 
                                    autocomplete="username"
                                    placeholder="tu@email.com"
                                >
                            </div>
                            
                            <div class="mb-4">
                                <label for="password" class="form-label fw-semibold">
                                    <i class="fas fa-lock me-2"></i>Contraseña
                                </label>
                                <input 
                                    type="password" 
                                    id="password" 
                                    name="_password" 
                                    class="form-control form-control-lg" 
                                    required 
                                    autocomplete="current-password"
                                    placeholder="Tu contraseña"
                                >
                            </div>
                            
                            <div class="mb-4">
                                <div class="form-check">
                                    <input 
                                        type="checkbox" 
                                        id="remember_me" 
                                        name="_remember_me" 
                                        class="form-check-input"
                                    >
                                    <label class="form-check-label" for="remember_me">
                                        Recordarme por 30 días
                                    </label>
                                </div>
                            </div>
                            
                            <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
                            
                            <button type="submit" class="btn btn-primary btn-lg w-100 mb-3">
                                <i class="fas fa-sign-in-alt me-2"></i>Iniciar Sesión
                            </button>
                        </form>
                        
                        <div class="text-center">
                            <a href="#" class="text-decoration-none">
                                <small>¿Olvidaste tu contraseña?</small>
                            </a>
                        </div>
                        
                        <hr class="my-4">
                        
                        <div class="text-center">
                            <p class="mb-3">¿No tienes una cuenta?</p>
                            <a href="{{ path('customer_register') }}" class="btn btn-outline-primary">
                                <i class="fas fa-user-plus me-2"></i>Crear Cuenta Gratis
                            </a>
                        </div>
                    </div>
                </div>
                
                <!-- Quick Login Benefits -->
                <div class="row mt-4">
                    <div class="col-6">
                        <div class="bg-white p-3 rounded shadow-sm text-center">
                            <i class="fas fa-heart fa-2x text-danger mb-2"></i>
                            <small class="d-block text-muted">Lista de Deseos</small>
                        </div>
                    </div>
                    <div class="col-6">
                        <div class="bg-white p-3 rounded shadow-sm text-center">
                            <i class="fas fa-history fa-2x text-info mb-2"></i>
                            <small class="d-block text-muted">Historial de Pedidos</small>
                        </div>
                    </div>
                </div>
            </div>
            
            <!-- Benefits Sidebar -->
            <div class="col-lg-4 col-md-5 mt-4 mt-lg-0">
                <div class="card border-0 bg-white shadow-sm">
                    <div class="card-header bg-success text-white">
                        <h5 class="mb-0">
                            <i class="fas fa-gift me-2"></i>Beneficios de tener cuenta
                        </h5>
                    </div>
                    <div class="card-body">
                        <div class="d-flex mb-3">
                            <div class="me-3">
                                <i class="fas fa-shipping-fast fa-2x text-primary"></i>
                            </div>
                            <div>
                                <h6 class="mb-1">Envío Gratis</h6>
                                <small class="text-muted">En compras superiores a $50</small>
                            </div>
                        </div>
                        
                        <div class="d-flex mb-3">
                            <div class="me-3">
                                <i class="fas fa-percent fa-2x text-success"></i>
                            </div>
                            <div>
                                <h6 class="mb-1">Descuentos Exclusivos</h6>
                                <small class="text-muted">Ofertas especiales para miembros</small>
                            </div>
                        </div>
                        
                        <div class="d-flex mb-3">
                            <div class="me-3">
                                <i class="fas fa-clock fa-2x text-warning"></i>
                            </div>
                            <div>
                                <h6 class="mb-1">Compra Rápida</h6>
                                <small class="text-muted">Datos guardados para checkout express</small>
                            </div>
                        </div>
                        
                        <div class="d-flex mb-3">
                            <div class="me-3">
                                <i class="fas fa-bell fa-2x text-info"></i>
                            </div>
                            <div>
                                <h6 class="mb-1">Notificaciones</h6>
                                <small class="text-muted">Alertas de productos y ofertas</small>
                            </div>
                        </div>
                        
                        <div class="d-flex">
                            <div class="me-3">
                                <i class="fas fa-headset fa-2x text-secondary"></i>
                            </div>
                            <div>
                                <h6 class="mb-1">Soporte Prioritario</h6>
                                <small class="text-muted">Atención preferencial 24/7</small>
                            </div>
                        </div>
                    </div>
                </div>
                
                <!-- Social Login -->
                <div class="card border-0 bg-white shadow-sm mt-3">
                    <div class="card-body text-center">
                        <h6 class="mb-3">O inicia sesión con:</h6>
                        <div class="d-grid gap-2">
                            <button class="btn btn-outline-danger">
                                <i class="fab fa-google me-2"></i>Google
                            </button>
                            <button class="btn btn-outline-primary">
                                <i class="fab fa-facebook-f me-2"></i>Facebook
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Footer -->
    <footer class="bg-dark text-white py-3 mt-5">
        <div class="container">
            <div class="row">
                <div class="col-md-6">
                    <small>&copy; {{ 'now'|date('Y') }} SFStore. Todos los derechos reservados.</small>
                </div>
                <div class="col-md-6 text-md-end">
                    <a href="#" class="text-white-50 text-decoration-none me-3">Ayuda</a>
                    <a href="#" class="text-white-50 text-decoration-none me-3">Términos</a>
                    <a href="#" class="text-white-50 text-decoration-none">Privacidad</a>
                </div>
            </div>
        </div>
    </footer>

    <!-- JavaScript -->
    {{ encore_entry_script_tags('app') }}
</body>
</html>